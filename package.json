{
  "name": "steal-framework",
  "version": "1.0.0",
  "description": "osu! beatmap plagiarism & cross-referencing forensics engine",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && rm -rf /tmp/steal-pages && mkdir -p /tmp/steal-pages && cp -r dist/* /tmp/steal-pages/ && cp dist/CNAME /tmp/steal-pages/ && touch /tmp/steal-pages/.nojekyll && git -C /tmp/steal-pages init && git -C /tmp/steal-pages config user.name 'Afterlight' && git -C /tmp/steal-pages config user.email 'afterlight@users.noreply.github.com' && git -C /tmp/steal-pages remote add origin https://github.com/Afterlight0338/steal-framework.git && git -C /tmp/steal-pages checkout -b gh-pages && git -C /tmp/steal-pages add -A && git -C /tmp/steal-pages commit -m 'deploy: publish steal.vivlos.dev' && git -C /tmp/steal-pages push origin gh-pages --force && rm -rf /tmp/steal-pages"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Afterlight0338/steal-framework.git"
  },
  "author": "Afterlight",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "lucide": "^0.475.0",
    "osu-beatmap-preview": "^0.3.0"
  }
}
